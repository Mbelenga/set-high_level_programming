#!/usr/bin/python3
"""Finds a peak in an unsorted integers"""


def find_peak(list_of_integers):
    """Returns a peak in a list of unsorted integers"""
    if list_of_integers == []:
        return None

    size = len(list_of_integers)
    if size == 1;
